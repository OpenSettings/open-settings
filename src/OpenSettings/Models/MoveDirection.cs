namespace OpenSettings.Models
{
    public enum MoveDirection
    {
        /// <summary>
        /// Move forward top | [-] decrease sort order.
        /// </summary>
        Up = 1,

        /// <summary>
        /// Move forward bottom | [+] increase sort order.
        /// </summary>
        Down = 2
    }
}